

@{
    ViewBag.Title = "Ventas";
}
@section Menu {
    @Scripts.Render("~/scripts/Tables/Data/menu")
}
@Styles.Render("~/AdminPacket/plugins/datatables/css")

@if (Session["users"] == null)
{
    Response.Redirect("~/Login/Index");

}

<div class="box-header">
    <a class="btn btn-success btn-lg pull-right"   href="/Sales/Create">
        <span class="glyphicon glyphicon-plus"></span> Nueva Factura
    </a>
</div>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Lista de Facturas</h3>
        </div>
        <div class="box-body">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>N°Factura</th>
                        <th>Nombre Completo</th>
                        <th>Monto</th>
                        <th>Estados</th>
                        <th>Fecha</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @*@foreach (var item in Model)
                {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                            <td>@Html.DisplayFor(modelItem => item.SecondName)</td>
                            <td>@Html.DisplayFor(modelItem => item.Name)</td>
                            <td>@Html.DisplayFor(modelItem => item.CellPhone)</td>
                            <td>@Html.DisplayFor(modelItem => item.Email)</td>
                            <td>
                                @if (Session["Type"] != null)
                                {
                                    <a class="btn btn-primary glyphicon glyphicon-edit" data-target="#modal-editar" data-toggle="modal" href="@Url.Action("Edit", "Clients", new { id = item.IDPerson })"></a>
                                    <a class="btn btn-danger glyphicon glyphicon-trash" data-target="#modal-editar" data-toggle="modal" href="@Url.Action("Delete", "Clients", new { id = item.IDPerson })"></a>
                                }
                                <a class="btn btn-info glyphicon glyphicon-file" data-target="#modal-details" data-toggle="modal" href="@Url.Action("Details", "Clients", new { id = item.IDPerson })"></a>
                                @if (true)
                                {

                                }
                                <a class="btn btn-warning glyphicon glyphicon-plus" data-target="#modal-details" data-toggle="modal" href="@Url.Action("CreateCP", "Clients", new { id = item.IDClient })"></a>
                            </td>
                        </tr>
                    }*@

                    <tr>
                        <td>183</td>
                        <td>Jose Carrillo Mendez</td>
                        <td>20,000,000.00</td>
                        <td><span class="label label-success">Cancelada</span></td>
                        <td>11-7-2014</td>
                        <td>
                            <a class="btn btn-info glyphicon glyphicon-file" data-target="#modal-editar" data-toggle="modal" href="@Url.Action("Delete", "Clients")"></a>
                            <a class="btn btn-danger glyphicon glyphicon-remove-sign" data-target="#modal-editar" data-toggle="modal" href="@Url.Action("Delete", "Clients")"></a>
                            <a class="btn btn-success glyphicon glyphicon-piggy-bank" data-target="#modal-editar" data-toggle="modal" href="@Url.Action("Delete", "Clients")"></a>
                        </td>
                    </tr>
                </tbody>
                <tfoot>
                    <tr>
                        <td>N°Factura</td>
                        <th>Nombre Completo</th>
                        <th>Monto</th>
                        <th>Estados</th>
                        <th>Fecha</th>
                        <th>Acciones</th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
}

<div class="modal fade" tabindex="-1" role="dialog" id="modal-delete" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
        </div>
    </div>
</div>

<div class="modal fade" tabindex="-1" role="dialog" id="modal-details" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
        </div>
    </div>
</div>


@section Scripts {
@Scripts.Render("~/AdminPacket/plugins/datatables/js")
@Scripts.Render("~/scripts/Tables/Data")
    <script src="~/scripts/jquery.unobtrusive-ajax.min.js"></script>
    <script>
        $('body').on('hidden.bs.modal', '.modal', function () {
            $(this).removeData('bs.modal');
        });
    </script>
}

<script>
    function closeModalDelete(data) {
        if (data.success) {
            $('#modal-delete').modal('hide');
            window.location.href = "/Sales/Index";
        }
    }
</script>

<script>
    function closeModalDetails(data) {
        if (data.success) {
            $('#modal-Details').modal('hide');
            window.location.href = "/Sales/Index";
        }
    }
</script>