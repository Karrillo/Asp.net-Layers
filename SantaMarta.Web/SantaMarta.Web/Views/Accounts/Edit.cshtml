@model SantaMarta.Data.Models.Accounts.Accounts

@{
    Layout = null;
    ViewBag.Title = "Edit";
}

@*modal*@
<div style="padding: 20px 30px; background: #00a65a; z-index: 999999; font-size: 16px; font-weight: 600;" class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
    <h4 class="modal-title" id="myModalLabel">Modificar Cuenta</h4>
</div>

<div class="modal-body">

    @using (Ajax.BeginForm("Edit", "Accounts", null, new AjaxOptions
    {

        HttpMethod = "POST",
        Url = Url.Action("Edit", "Accounts"),
        UpdateTargetId = "modal-content",
        OnSuccess = "closeModalEdit",
        InsertionMode = InsertionMode.Replace

    },
        new { @class = "form-horizontal", role = "form", id = "frmEdit" }))
    {

        @Html.AntiForgeryToken()

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IDAccount)

        //textbox
        <div class="box-body">
            Nombre de Cuenta:
            @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control", Type = "text", placeholder = "Nombre de cuenta" } })
            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-danger" data-dismiss="modal">Cancelar</button>
            <input type="submit" value="Guardar" class="btn btn-success" />
        </div>
    }
</div>

<script src="~/scripts/jquery.validate.min.js"></script>
<script src="~/scripts/jquery.validate.unobtrusive.min.js">
<
